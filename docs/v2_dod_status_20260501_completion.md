# v2 Definition of Done — Completion Pass Status Report

**Date:** 2026-05-01  
**Auditor:** Claude Sonnet 4.6  
**Branch:** `main` @ commit `faa1305`  
**Reference run:** `market_analysis_couples_therapy_20260501_0828`  
**Tests:** 340 passed, 5 skipped (as of faa1305)  
**Spec:** `serp_tool1_completion_spec.md`

This document is the mandatory first deliverable per Rule 1 of the completion spec. It covers the status of every v2 DoD item, every fix from `serp_tool1_fix_spec.md`, and every item from this completion spec (M1, M2, M3).

---

## Prior-pass fixes — status at faa1305

### Fix 1 — Confidence denominator + evidence fields

**Status: DONE**  
**File:** `intent_verdict.py`, `generate_content_brief.py`  
**Commit:** faa1305  
**Evidence:** `output/market_analysis_couples_therapy_20260501_0828.json` `keyword_profiles["couples counselling"]["serp_intent"]["confidence"]` = `"medium"` (was `"low"` in 0717 run); evidence fields are `organic_url_count`, `classified_organic_url_count`, `uncategorised_organic_url_count`; `intent_counts` key absent.

### Fix 2 — Integer intent_distribution

**Status: DONE**  
**File:** `intent_verdict.py`  
**Commit:** faa1305  
**Evidence:** `keyword_profiles["couples counselling"]["serp_intent"]["intent_distribution"]` = `{"informational": 4, "local": 4}` — integer values confirmed.

### Fix 3 — Competitor handoff tests + docs

**Status: DONE**  
**File:** `test_handoff_schema.py`, `docs/handoff_contract.md`, `serp_audit.py`  
**Commit:** faa1305  
**Evidence:** `test_no_organic_returns_none`, `test_all_client_urls_produces_empty_targets`, `test_invalid_handoff_fails_validation` all pass. `docs/handoff_contract.md` exists.

### Fix 4 — Report rendering (xlsx)

**Status: DONE**  
**File:** `serp_audit.py`  
**Commit:** faa1305  
**Evidence:** `SERP_Intent_Detail` sheet added to xlsx; `Primary_Intent` and `Intent_Confidence` columns injected into `all_metrics` loop.

### Fix 4 — Report rendering (markdown)

**Status: PARTIAL**  
**File:** `generate_content_brief.py` — `generate_serp_intent_section()` exists at line 2122  
**Commit:** faa1305  
**What is done:** `generate_serp_intent_section()` function exists and is called within `generate_content_brief.py`'s list-mode report (content_opportunities flow), prepended to the LLM output at line 2414.  
**What is missing (M1 of completion spec):**
- `market_analysis_*.md` (produced by `serp_audit.py` via `generate_insight_report.py`) does NOT contain a `## 5b. Per-Keyword SERP Intent` section. The function `generate_insight_report.py` has no knowledge of `keyword_profiles` and calls no SERP intent rendering.
- No Mixed-Intent Strategic Note callout exists in Section 4 of `market_analysis_*.md`.
- No `## 1a. SERP Intent Context` subsection exists in the `## Brief N:` sections (generated by `generate_content_brief.generate_brief()` at line 2504).

**Discrepancy flagged (Rule: flag before resolving):**  
`generate_insight_report.py:187` already contains `## 5b. Keyword Feasibility & Pivot Recommendations`. The completion spec requires `## 5b. Per-Keyword SERP Intent`. If both are rendered in the same run, two `## 5b` sections would appear. In the `0828` run the feasibility section was absent (no `keyword_feasibility` data), so the conflict did not manifest. However, when feasibility is run, both labels would appear.  
**Proposed resolution:** Rename the existing feasibility section to `## 5c. Keyword Feasibility & Pivot Recommendations` and add the new `## 5b. Per-Keyword SERP Intent` section directly after Section 5 SERP Composition. This preserves the ordering intent of both sections and satisfies the acceptance criteria regex `Section 5b\. Per-Keyword SERP Intent`. The feasibility section renaming is a one-line change in `generate_insight_report.py:187`.

### Fix 5 — Classifier audit + URL pattern rules

**Status: DONE**  
**Files:** `scripts/classifier_audit.py`, `url_pattern_rules.yml`, `docs/classifier_audit_couples_therapy.md`, `docs/classifier_residual_20260501.md`  
**Commit:** faa1305  
**Evidence:** `scripts/classifier_audit.py` exists and is runnable. `docs/classifier_audit_couples_therapy.md` contains total URL count (168), `other` count (27 = 16.1%), top domains contributing `other`.

### Fix 6 — Intent mapping rationale

**Status: PARTIAL**  
**File:** `docs/intent_mapping_rationale.md`, `intent_mapping.yml`  
**Commit:** faa1305  
**What is done:** `docs/intent_mapping_rationale.md` exists with rule-by-rule rationale and decision/reasoning for all 3 required edge cases. `intent_mapping.yml` exists at repo root.  
**What is missing (M2.B):** Each edge case requires "one example URL from the fixture run that exemplifies the case." The current rationale doc uses generic placeholder examples, not URLs from the `0828` fixture run. Must add:
- EC1 (service on directory): `https://www.psychologytoday.com/ca/therapists/bc/vancouver?category=couples` — directory entity, classified as `guide` by URL pattern (the listing is the archetypal Psychology Today therapist-search page)
- EC2 (guide on counselling + local pack): `https://openspacecounselling.ca/` — entity_type=counselling, content_type=guide (URL pattern), keyword="What type of therapist is best for couples therapy?" which has `local_pack_present: True`; maps to `informational`, not `local`
- EC3 (AI Overview): keyword "How much is couples therapy in Vancouver?" has `aio_citation_count: 11` (from `ai_overview_citations`); AI Overview presence does not affect intent classification

### Fix 7 — Validator rule updates

**Status: PARTIAL**  
**File:** `generate_content_brief.py`  
**Commit:** faa1305  
**What is done:** `dominant_pattern` contradiction is HARD-FAIL; `confidence` upgrade contradiction is SOFT-FAIL; five tests exist (one per field).  
**What is missing (M2.C):** `docs/validator_rules_<date>.md` has never been created. Required by completion spec M2.C.

### Fix 8 — README and documentation

**Status: DONE**  
**File:** `README.md`, `docs/handoff_contract.md`  
**Commit:** faa1305  
**Evidence:** README contains "What's new in this version (v2)", "Backwards compatibility" section, "Tool 1 → Tool 2 handoff" subsection pointing to `docs/handoff_contract.md`.

---

## Completion spec items — status at start of this pass

### M1 — Markdown rendering

**Status: NOT DONE**  
See Fix 4 markdown partial above. All three sub-items (M1.A, M1.B, M1.C) require implementation.

### M2.A — Classifier audit output

**Status: DONE** (see Fix 5 above)  
`docs/classifier_audit_couples_therapy.md` exists with required content. No separate date-stamped file for this pass needed (output is the same fixture).

### M2.B — Intent mapping rationale with fixture URLs

**Status: PARTIAL** (see Fix 6 above)  
The rationale doc exists but lacks fixture URLs. Will be updated in this pass.

### M2.C — Validator rules doc

**Status: NOT DONE**  
`docs/validator_rules_20260501.md` does not exist. Will be created in this pass.

### M2.D — README and documentation

**Status: DONE** (see Fix 8 above)

### M3 — Handoff schema clarification

**Status: NOT DONE**  
Inspection of `output/competitor_handoff_couples_therapy_20260501_0832.json`:
- Total targets: 46  
- `source_keyword == primary_keyword_for_url`: 42 of 46  
- Differ in 4 cases:

  | URL (truncated) | source_keyword | primary_keyword_for_url |
  |-----------------|---------------|------------------------|
  | `https://counselling-vancouver.com/` | "How much is couples therapy in Vancouver?" | "success rate of couples therapy?" |
  | `https://www.psychologytoday.com/ca/therapists/bc/v…` | "How much is couples therapy in Vancouver?" | "What type of therapist is best for couples therapy?" |
  | `https://www.nofearcounselling.com/why-consider-cou…` | "success rate of couples therapy?" | "how does couples counselling work" |
  | `https://wellbeingscounselling.ca/psychotherapy/ser…` | "success rate of couples therapy?" | "how does couples counselling work" |

**Decision: Option A** — the two fields hold different values for some targets. `source_keyword` is the keyword for which the URL was found in this particular entry; `primary_keyword_for_url` is the keyword for which the URL had its best (lowest) rank across all keywords. These are semantically different. Will document in `docs/handoff_contract.md`.

---

## Items being implemented in this pass (in order)

1. **M1.A** — Add `## 5b. Per-Keyword SERP Intent` to `generate_insight_report.py` (between Section 5 and Section 6). Rename existing feasibility section to `## 5c`.  
   Files: `generate_insight_report.py`

2. **M1.B** — Add Mixed-Intent Strategic Note callout at top of Section 4 in `generate_insight_report.py`.  
   Files: `generate_insight_report.py`

3. **M1.C** — Add `## 1a. SERP Intent Context` to `generate_content_brief.generate_brief()` before `## 1. The Core Conflict`.  
   Files: `generate_content_brief.py`

4. **M2.B** — Update `docs/intent_mapping_rationale.md` with fixture URLs for each edge case.  
   Files: `docs/intent_mapping_rationale.md`

5. **M2.C** — Create `docs/validator_rules_20260501.md`.  
   Files: `docs/validator_rules_20260501.md`

6. **M3** — Update `docs/handoff_contract.md` with Option A documentation.  
   Files: `docs/handoff_contract.md`

7. **Tests** — Create `tests/test_markdown_rendering.py` (acceptance criteria from M1).

---

## Discrepancy log

| # | Location | Description | Proposed resolution |
|---|----------|-------------|-------------------|
| 1 | `generate_insight_report.py:187` | `## 5b` is already used for "Keyword Feasibility"; completion spec requires `## 5b` for "Per-Keyword SERP Intent" | Rename feasibility to `## 5c`; add SERP Intent as `## 5b` |
