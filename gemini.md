# Tactical Rules: serp-keyword

## 📡 Discovery Standards
- **Feasibility First:** Use the `shared_config.json` Client DA (35) to calculate the "Hyper-Local Pivot" need.
- **Aggregator Bypass:** Automatically flag domains in the `filtering.aggregators` list as "Noise" to prevent downstream credit waste.
- **Data Export:** Ensure `market_analysis.json` includes the 'Search Intent' (PAA) for use by the Sniper app.